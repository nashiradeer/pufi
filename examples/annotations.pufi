@NoIgnore
pub enum Result<T, E> {
    Ok(T),
    Err(E)
}

@NoIgnore
pub enum Option<T> {
    Some(T),
    None
}

@NoClone
pub struct CantBeCloned { }

@Runtime
pub func open_file(path: string): File;