@forceUsage
@optionalClone
pub enum Result<T, E> {
    Ok(T),
    Err(E)
}

@forceUsage
@optionalClone
pub enum Option<T> {
    Some(T),
    None
}

@disableClone
pub struct CantBeCloned { }

@runtimeOnly
pub func open_file(path: string): File;