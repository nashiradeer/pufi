import std.fs.File as OsFile;
import std.io.Write;
import std.io.Read;
import std.array.count_items;
import std.bin.Entry;

@Entry
pub func main() {
    var file = new OsFile("file.txt");
    var file2 = new OsFile("file2.txt);

    const buffer = file.read_all();
    file2.write_all(buffer);
}