pub enum Result<T, E> {
    Ok(T),
    Err(E)
}

pub enum Option<T> {
    None,
    Some(T)
}

pub enum Operations {
    Add = 0,
    Subtract = 1,
    Multiply = 2,
    Division = 3,
    Number(integer) = 4
}